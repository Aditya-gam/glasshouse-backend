# LiteLLM Proxy config — STUB for local dev (compose profile: gateway).
#
# TODO(M1.5): the real gateway config — model slots
# (profiler / vlm / anonymizer / adversary / feedback-adversary / judge / inpaint / tagger),
# local↔cloud profiles, per-slot fallbacks, budget caps (virtual keys), and the startup
# separation assertion (editor ≠ judge ≠ adversary).
# Spec: ../Privacy-Exposure-App/docs/04-ai-engine/llm-gateway.md
#
# One placeholder slot so the proxy starts when the `gateway` profile is brought up.
model_list:
  - model_name: profiler
    litellm_params:
      model: ollama/llama3.2
      api_base: http://ollama:11434

litellm_settings:
  drop_params: true
