"""Per-source adapters (upload/connector/loader) that conform to `base.SourceAdapter`.

Concrete adapters (X archive, Reddit export, Google Takeout, photos; live connectors) land at
M1.4+ — see `03-data/ingestion/sources/*`. M1.1 ships only the port they implement.
"""
